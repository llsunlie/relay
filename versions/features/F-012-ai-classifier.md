# F-012 文件内容 AI 分类器

**里程碑**: v0.0.3 (AI 识别)

## 描述

读取文件内容（前 4096 字符 + 文件名 + 元数据）进行类型判断。分类体系涵盖周报、作业/论文、请假申请、报销单、设计稿、代码提交、会议纪要、合同、发票、通用文档。本地推理保护隐私。支持 `.md` `.txt` `.pdf` `.docx` `.xlsx` `.pptx` 及图片 OCR。

## 依赖

- F-007 文件夹监听引擎

## 成功标准

- 自动分类准确率 > 85%
- 本地推理延迟 < 2 秒
