# F-023 格式转换管线

**里程碑**: v0.1.1 (格式转换)

## 描述

可插拔的转换管线：`原始格式 → 结构提取 → 语义IR → 目标模板 → 目标格式`。第一期支持 Markdown ↔ HTML/PDF/纯文本、CSV → 图表 PNG、任意文本 → Markdown 规范化。基于 Pandoc + 自研模板引擎。

## 依赖

- F-022 格式偏好声明

## 成功标准

- 基础转换路径可用（md→html, md→pdf, csv→png, txt→md）
- 单次转换 < 3 秒
