# F-026 通道抽象层

**里程碑**: v0.2.0 (多通道)

## 描述

统一通道接口，新增通道只需实现适配器：`DeliveryChannel` 接口定义 `name`、`capabilities`、`send()`、`checkStatus()`。

## 依赖

- F-005 邮件投递

## 成功标准

- 新增通道适配器 < 100 行代码
- 现有邮件通道迁移到抽象层后行为不变
