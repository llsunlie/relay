# F-020 接收方格式偏好

**里程碑**: v0.1.1 (格式转换)

## 用户故事

作为接收方，我在收件箱设置中声明每种交付类型的偏好格式，发送方在发送前自动按我喜欢的格式转换。

## 设置入口

系统托盘右键 → 设置 → 格式偏好。

## 偏好设置

按交付类型（周报、作业提交、请假申请、报销申请、通用）分别选择偏好格式（Markdown、PDF、DOCX、TXT），默认「保持原格式」。

## 使用方式

发送方发送前查询接收方的格式偏好，若有指定格式则按偏好转换后发送。

## 成功标准

- 偏好设置可保存和同步
- 发送方可正确查询到偏好
